# Rendering Concept Art

The concept art can be rendered in the terminal with `cat [filename.txt]` e.g. `cat menu.txt` followed by `Windows + PrtSc` or equivalent. This does preclude the possibility of colored text in the renders, but strikes the best balance between clarity and efficiency.

*NOTE: This **is** in fact sarcasm*